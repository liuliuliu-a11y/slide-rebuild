# PPT 复刻当前页检查表

阈值与判断标准只维护在 acceptance.md；本清单用于执行，不增加另一套审批流程。

## 输入与冻结

- 当前仅一个 page_id；页序、page_type、page_control 已明确。
- generated-page 已复用上游任务书、准确文字/数据、冻结图和真实生成回执。
- generated-page 的父流程处于复刻阶段；整套图片检查通过，目录/任务书/冻结图片版本与实际文件一致，未将单页 image_ready 当作整套通过。
- external-reference 已读取原文件并核对缺项，不把 OCR 当无误事实。
- 封面与内容页使用各自的页面任务和验收重点。
- 原生对象、图片层、关系端点、背景保真、输出与资产目录已冻结。
- Planner 只补重建所需信息，未重新编造文稿或数据。
- 已确定复用构建源/工具与实际能力；未因新页重写通用逻辑，未把候选工具称为已部署。
- 坐标单位、文字测量依据、样式分组与换行风险已明确；已有 brief/manifest 直接复用。

## 构建与素材

- 文字/数字/单位/脚注为原生文本；结构/关系可编辑。
- 表格为真表格；科研曲线附数据与 MATLAB/Python 脚本并核对导出图；仅明确要求在 PPT 内改数据的图表检查原生图表与嵌入数据。
- 没有整页贴图、重复文字背景、轮廓文字或包 PNG 的 SVG 冒充编辑。
- 复杂视觉为独立无字资产；已有合格生成素材或授权原始素材已按证据复用。
- 去字/分层编辑引用冻结图，设备身份、视角与构图保持一致。
- manifest 记录来源/回执/请求与实际型号、尺寸/PPI、边缘与无字检查。
- contact sheet 已覆盖并检查全部使用资产，最终摆放无模糊、变形或粗糙裁切。
- 已认可主视觉得到保留；接受的简化有明确记录。
- 可重复构建源和项目内素材已保存。
- 主要模块有稳定对象映射，分组/端点连接符合编辑需要；公式表示方式如实标明。

## 验证与复核

- 真实 PowerPoint 预览与最终文件同一版本，文字/关系/层次已逐项核对。
- 已按 acceptance.md 第 7 节确定新页/局部修改/工具变更范围。
- 必要文字、对象、表格/图表数据已按范围读回；沿用证据有未受影响依据。
- 适用的副本文字/模块/数据编辑保存重开通过，旧测试未冒称本轮全量实测。
- 质量差距落实到区域和现象，未用对象总数、耗时或像素相似度替代质量判断。
- 只读 reviewer 已查看图、任务书、资产和编辑证据；不可用时明确 fallback。
- Blocking 已修复并复测，修改后重新导出；没有问题不追加空转评审。
- 正式用途卫生检查通过，未扫描或工具限制如实报告。
- 生成 PPTX/预览在 output，资产/源码/回执/QA 在项目/work，源文件不变。

## checkpoint 与合并

- 本页 PPTX、预览、资产/源码索引、QA、差距和状态已保存。
- page_control 已按父任务或独立请求继承；checkpoint 模式已展示当前页并按用户要求等待。
- continuous_serial 在已有完整或阶段授权下推进，本页验收后才复刻下一页，不切回逐页整页生图。
- 合并只包含已验收页面；页数/顺序、实际渲染、原生对象和媒体/数据关系已再核验。
- 交付说明区分本页完成、全套完成、视觉样张和未能确认项目。
- 普通页面任务未重复运行整组基准；共用工具变更按受影响类型验证，未将规则修改称为效果提升实测。
